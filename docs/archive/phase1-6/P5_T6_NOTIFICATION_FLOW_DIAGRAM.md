# Notification System - Flow Diagrams

Visual diagrams showing notification flows and interactions.

---

## 🔄 Overall System Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                         EVENT TRIGGERS                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  Task Reminder    Task Failure    Agent Crash    User Action        │
│  (15 min before)  (Exception)     (Unexpected)   (Settings saved)   │
│       │                │                │              │             │
│       └────────────────┴────────────────┴──────────────┘             │
│                              │                                        │
│                    ┌─────────▼─────────┐                            │
│                    │ NOTIFICATION LOGIC │                            │
│                    │                    │                            │
│                    │ 1. Check user prefs│                            │
│                    │ 2. Select channels │                            │
│                    │ 3. Format messages │                            │
│                    └─────────┬─────────┘                            │
│                              │                                        │
│         ┌────────────────────┼────────────────────┐                 │
│         │                    │                    │                  │
│    ┌────▼────┐         ┌────▼────┐         ┌────▼────┐            │
│    │  EMAIL  │         │  PUSH   │         │WEBSOCKET│            │
│    │ SERVICE │         │ SERVICE │         │BROADCAST│            │
│    └────┬────┘         └────┬────┘         └────┬────┘            │
│         │                    │                    │                  │
└─────────┼────────────────────┼────────────────────┼─────────────────┘
          │                    │                    │
     ┌────▼────┐          ┌───▼────┐          ┌───▼────┐
     │  SMTP   │          │Web Push│          │WebSocket│
     │ Server  │          │  API   │          │Connected│
     └────┬────┘          └───┬────┘          └───┬────┘
          │                    │                    │
     ┌────▼────┐          ┌───▼────┐          ┌───▼────┐
     │ User's  │          │Browser │          │Browser │
     │  Inbox  │          │Push    │          │  Tab   │
     └─────────┘          └────────┘          └────────┘
```

---

## 📧 Email Notification Flow

```
┌─────────────────────────────────────────────────────────┐
│           TASK REMINDER EMAIL FLOW                      │
└─────────────────────────────────────────────────────────┘

[Scheduler: Every 1 minute]
         │
         ▼
┌────────────────────────┐
│ Query Database:        │
│ - next_run_at between  │
│   13-17 min from now   │
│ - reminder_sent=false  │
│ - user pref enabled    │
└──────────┬─────────────┘
           │
           ▼
    ┌──────────────┐
    │ For each task│
    └──────┬───────┘
           │
           ▼
┌──────────────────────┐
│ Generate HTML email: │
│ - Task name          │
│ - Schedule time      │
│ - Dashboard link     │
│ - Professional style │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Send via SMTP:       │
│ - Connect to server  │
│ - Authenticate       │
│ - Send message       │
│ - Disconnect         │
└──────────┬───────────┘
           │
           ├─── Success ──────┐
           │                  │
           │                  ▼
           │         ┌─────────────────┐
           │         │ Update Database:│
           │         │ reminder_sent   │
           │         │ = true          │
           │         └─────────────────┘
           │
           └─── Error ───────┐
                             │
                             ▼
                    ┌─────────────────┐
                    │ Log error       │
                    │ Retry later     │
                    └─────────────────┘
```

---

## 🔔 Push Notification Flow

```
┌─────────────────────────────────────────────────────────┐
│         BROWSER PUSH NOTIFICATION FLOW                   │
└─────────────────────────────────────────────────────────┘

[SETUP PHASE - One-time per device]

User clicks "Enable Push"
         │
         ▼
┌─────────────────────┐
│ Request permission  │
│ Notification.       │
│ requestPermission() │
└──────────┬──────────┘
           │
           ├─── Granted ──────┐
           │                  │
           │                  ▼
           │         ┌──────────────────┐
           │         │ Get service      │
           │         │ worker ready     │
           │         └────────┬─────────┘
           │                  │
           │                  ▼
           │         ┌──────────────────┐
           │         │ Subscribe to     │
           │         │ push manager     │
           │         │ with VAPID key   │
           │         └────────┬─────────┘
           │                  │
           │                  ▼
           │         ┌──────────────────┐
           │         │ Send subscription│
           │         │ to backend API   │
           │         └────────┬─────────┘
           │                  │
           │                  ▼
           │         ┌──────────────────┐
           │         │ Store in database│
           │         │ push_subscriptions│
           │         └──────────────────┘
           │
           └─── Denied ──────┐
                             │
                             ▼
                    ┌─────────────────┐
                    │ Show error toast│
                    └─────────────────┘

[SEND PHASE - Each notification]

Critical event occurs (task failure, agent crash)
         │
         ▼
┌──────────────────────┐
│ Check user prefs:    │
│ browser_notifications│
│ = true               │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Get all subscriptions│
│ for user_id          │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ For each device:     │
│ - Create payload     │
│ - Sign with VAPID    │
│ - Send via Web Push  │
└──────────┬───────────┘
           │
           ├─── Success (200) ─────┐
           │                        │
           │                        ▼
           │               ┌─────────────────┐
           │               │ Notification    │
           │               │ delivered       │
           │               └─────────────────┘
           │
           ├─── Gone (410) ────────┐
           │                       │
           │                       ▼
           │              ┌─────────────────┐
           │              │ Remove expired  │
           │              │ subscription    │
           │              └─────────────────┘
           │
           └─── Error ────────────┐
                                  │
                                  ▼
                         ┌─────────────────┐
                         │ Log error       │
                         │ Continue to next│
                         └─────────────────┘
```

---

## 💬 WebSocket Toast Flow

```
┌─────────────────────────────────────────────────────────┐
│           WEBSOCKET TOAST NOTIFICATION FLOW              │
└─────────────────────────────────────────────────────────┘

[CONNECTION PHASE]

User opens app
      │
      ▼
┌──────────────────┐
│ React component  │
│ useNotifications │
│ hook initializes │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Create WebSocket │
│ connection to    │
│ ws://backend/ws  │
└────────┬─────────┘
         │
         ├─── Connected ────┐
         │                  │
         │                  ▼
         │         ┌──────────────────┐
         │         │ Register user_id │
         │         │ with connection  │
         │         │ manager          │
         │         └──────────────────┘
         │
         └─── Failed ───────┐
                            │
                            ▼
                   ┌─────────────────┐
                   │ Retry in 3 sec  │
                   └─────────────────┘

[NOTIFICATION PHASE]

Event occurs (task started, completed, failed)
         │
         ▼
┌──────────────────────┐
│ Check user prefs:    │
│ websocket_notifications│
│ = true               │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Create notification: │
│ - id, type, title    │
│ - message, duration  │
│ - action, data       │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Send via WebSocket:  │
│ JSON message to      │
│ user's connections   │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Frontend receives:   │
│ - Parse JSON         │
│ - Extract notification│
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Show toast:          │
│ - Type-based styling │
│ - Auto-dismiss timer │
│ - Action button      │
└──────────┬───────────┘
           │
           ├─── User clicks action ──┐
           │                         │
           │                         ▼
           │                ┌─────────────────┐
           │                │ Navigate to URL │
           │                │ Dismiss toast   │
           │                └─────────────────┘
           │
           └─── Timer expires ───────┐
                                     │
                                     ▼
                            ┌─────────────────┐
                            │ Auto-dismiss    │
                            │ Remove from DOM │
                            └─────────────────┘
```

---

## 🎯 User Preference Flow

```
┌─────────────────────────────────────────────────────────┐
│           NOTIFICATION PREFERENCES FLOW                  │
└─────────────────────────────────────────────────────────┘

User navigates to Settings → Notifications
         │
         ▼
┌──────────────────────┐
│ Load preferences     │
│ from database        │
│ (GET /api/           │
│ notifications/       │
│ preferences)         │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Display UI:          │
│ ☑ Email notifications│
│ ☑ Push notifications │
│ ☑ Toast notifications│
│   ☑ Reminders        │
│   ☑ Failures         │
│   ☑ Weekly summary   │
└──────────┬───────────┘
           │
User toggles preferences
           │
           ▼
┌──────────────────────┐
│ Update local state   │
│ (React useState)     │
└──────────┬───────────┘
           │
User clicks "Save"
           │
           ▼
┌──────────────────────┐
│ Send to backend:     │
│ PUT /api/            │
│ notifications/       │
│ preferences          │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Backend validates    │
│ and saves to         │
│ user_preferences     │
└──────────┬───────────┘
           │
           ├─── Success ──────┐
           │                  │
           │                  ▼
           │         ┌─────────────────┐
           │         │ Show success    │
           │         │ toast           │
           │         └─────────────────┘
           │
           └─── Error ───────┐
                             │
                             ▼
                    ┌─────────────────┐
                    │ Show error toast│
                    │ Revert changes  │
                    └─────────────────┘

[FILTER PHASE - When sending notification]

Notification event occurs
         │
         ▼
┌──────────────────────┐
│ Query user_prefs:    │
│ SELECT * FROM        │
│ user_preferences     │
│ WHERE user_id = ?    │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Check channel prefs: │
│ - email_notifications│
│ - browser_notifications│
│ - websocket_notifications│
└──────────┬───────────┘
           │
           ├─── Enabled ──────┐
           │                  │
           │                  ▼
           │         ┌─────────────────┐
           │         │ Check event type│
           │         │ preferences:    │
           │         │ - reminder_...  │
           │         │ - failure_...   │
           │         │ - weekly_...    │
           │         └────────┬────────┘
           │                  │
           │                  ├─── Enabled ──────┐
           │                  │                   │
           │                  │                   ▼
           │                  │          ┌─────────────────┐
           │                  │          │ Send notification│
           │                  │          └─────────────────┘
           │                  │
           │                  └─── Disabled ─────┐
           │                                     │
           │                                     ▼
           │                            ┌─────────────────┐
           │                            │ Skip notification│
           │                            └─────────────────┘
           │
           └─── Disabled ─────┐
                              │
                              ▼
                     ┌─────────────────┐
                     │ Skip notification│
                     └─────────────────┘
```

---

## ⏰ Scheduler Flow

```
┌─────────────────────────────────────────────────────────┐
│            APSCHEDULER BACKGROUND JOBS                   │
└─────────────────────────────────────────────────────────┘

[INITIALIZATION]

FastAPI app starts
      │
      ▼
┌──────────────────┐
│ Start scheduler  │
│ (APScheduler)    │
└────────┬─────────┘
         │
         ├─── Job 1: Task Reminders (every 1 min) ──┐
         │                                           │
         ├─── Job 2: Weekly Summaries (Mon 9 AM) ───┤
         │                                           │
         └─── Job 3: Cleanup (daily 3 AM) ──────────┤
                                                     │
                                                     ▼
                                            ┌─────────────────┐
                                            │ Jobs registered │
                                            │ and running     │
                                            └─────────────────┘

[JOB 1: TASK REMINDERS - Every 1 minute]

Cron trigger fires
      │
      ▼
┌──────────────────┐
│ Query tasks:     │
│ WHERE next_run_at│
│ BETWEEN NOW+13m  │
│ AND NOW+17m      │
│ AND reminder_sent│
│ = false          │
└────────┬─────────┘
         │
         ▼
For each task:
    ├─ Check user preferences
    ├─ Send email reminder
    ├─ Mark reminder_sent = true
    └─ Log result


[JOB 2: WEEKLY SUMMARIES - Mondays 9 AM]

Cron trigger fires
      │
      ▼
┌──────────────────┐
│ Query users with │
│ weekly_summary   │
│ = true           │
└────────┬─────────┘
         │
         ▼
For each user:
    ├─ Get completed tasks (last 7 days)
    ├─ Get upcoming tasks (next 7 days)
    ├─ Generate HTML email
    ├─ Send summary email
    └─ Log result


[JOB 3: CLEANUP - Daily 3 AM]

Cron trigger fires
      │
      ▼
┌──────────────────┐
│ Reset flags:     │
│ UPDATE tasks     │
│ SET reminder_sent│
│ = false          │
│ WHERE next_run_at│
│ > NOW+1h         │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Log cleanup      │
│ results          │
└──────────────────┘
```

---

**Next**: See implementation details in `P5_T6_NOTIFICATIONS_IMPLEMENTATION.md`
